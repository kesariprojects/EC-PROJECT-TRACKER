/* __imports_rewritten__ */
import React from 'https://esm.sh/react@19.2.0';
import { Link } from 'https://esm.sh/react-router-dom@7.13.0?deps=react@19.2.0';
import { html } from '../jsx.js';
import { PageTitle, EmptyState } from '../components/UI.js';
import { useAppShell } from '../mainProviders.js';
import { getMetrics } from '../utils/metrics.js';

export function Clients() {
  const { session, projects, owners, createProject, updateProject, deleteProject } = useAppShell();
  const blank = { clientName:'', projectOwner:owners[0] || 'Others', newOwner:'', mepInitial:'', architectInitial:'', projectName:'', location:'', projectDetails:'', startDate:'', endDate:'' };
  const [form, setForm] = React.useState(blank);
  const [editing, setEditing] = React.useState(null);
  const [editForm, setEditForm] = React.useState(null);
  if (session?.role !== 'Admin') return html`<${EmptyState} title="Admin access required" detail="Client portfolio is available only for authenticated admins." />`;
  const ownerValue = f => f.projectOwner === '__new__' ? f.newOwner : f.projectOwner;
  const submit = e => { e.preventDefault(); createProject({ ...form, projectOwner: ownerValue(form) }); setForm(blank); };
  const saveEdit = e => { e.preventDefault(); updateProject(editing, { ...editForm, projectOwner: ownerValue(editForm) }); setEditing(null); setEditForm(null); };
  const remove = code => { if (confirm('Are you sure you want to delete this project?')) deleteProject(code); };
  const ownerSelect = (state, setState) => html`<select required value=${state.projectOwner} onChange=${e=>setState({...state,projectOwner:e.target.value})} className="rounded-2xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-3 md:col-span-2">${owners.map(o => html`<option key=${o} value=${o}>${o}</option>`)}<option value="__new__">Add New Owner</option></select>`;
  return html`<div><${PageTitle} eyebrow="Clients" title="Project Management" description="Create, edit, delete, and open project folders. Client Codes are generated once and locked." /><form onSubmit=${submit} className="card mb-6 grid gap-3 p-5 md:grid-cols-6"><input required placeholder="Client Name" value=${form.clientName} onInput=${e=>setForm({...form,clientName:e.target.value})} className="rounded-2xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-3 md:col-span-2" />${ownerSelect(form, setForm)}${form.projectOwner === '__new__' && html`<input required placeholder="New Project Owner" value=${form.newOwner} onInput=${e=>setForm({...form,newOwner:e.target.value})} className="rounded-2xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-3 md:col-span-2" />`}<input required placeholder="MEP Name" value=${form.mepInitial} onInput=${e=>setForm({...form,mepInitial:e.target.value})} className="rounded-2xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-3" /><input required placeholder="Architect Name" value=${form.architectInitial} onInput=${e=>setForm({...form,architectInitial:e.target.value})} className="rounded-2xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-3" /><input required placeholder="Project Name" value=${form.projectName} onInput=${e=>setForm({...form,projectName:e.target.value})} className="rounded-2xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-3 md:col-span-2" /><input placeholder="Location" value=${form.location} onInput=${e=>setForm({...form,location:e.target.value})} className="rounded-2xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-3 md:col-span-2" /><textarea placeholder="Project Details" value=${form.projectDetails} onInput=${e=>setForm({...form,projectDetails:e.target.value})} className="rounded-2xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-3 md:col-span-4"></textarea><button className="rounded-2xl bg-[hsl(var(--primary))] p-3 font-semibold text-white md:col-span-2">Create New Project</button></form>${projects.length === 0 ? html`<${EmptyState} title="No projects yet" detail="Create a client project to generate a permanent client code." />` : html`<div className="grid gap-4 md:grid-cols-3">${projects.map(p => { const m = getMetrics([p]); return html`<div key=${p.code} className="card card-hover p-6">${editing === p.code ? html`<form onSubmit=${saveEdit} className="space-y-3"><p className="text-sm font-semibold">Client Code: ${p.code}</p><input required value=${editForm.projectName} onInput=${e=>setEditForm({...editForm,projectName:e.target.value})} className="w-full rounded-2xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-3" />${ownerSelect(editForm, setEditForm)}${editForm.projectOwner === '__new__' && html`<input required placeholder="New Project Owner" value=${editForm.newOwner || ''} onInput=${e=>setEditForm({...editForm,newOwner:e.target.value})} className="w-full rounded-2xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-3" />`}<input type="date" value=${editForm.startDate || ''} onInput=${e=>setEditForm({...editForm,startDate:e.target.value})} className="w-full rounded-2xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-3" /><input type="date" value=${editForm.endDate || ''} onInput=${e=>setEditForm({...editForm,endDate:e.target.value})} className="w-full rounded-2xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-3" /><textarea value=${editForm.projectDetails || ''} onInput=${e=>setEditForm({...editForm,projectDetails:e.target.value})} className="w-full rounded-2xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-3"></textarea><div className="flex gap-2"><button className="rounded-2xl bg-[hsl(var(--primary))] px-4 py-2 text-sm font-semibold text-white">Save</button><button type="button" onClick=${()=>setEditing(null)} className="rounded-2xl border border-[hsl(var(--border))] px-4 py-2 text-sm font-semibold">Cancel</button></div></form>` : html`<div><${Link} to=${`/projects/${p.code}`} className="block"><div className="mb-4 inline-flex rounded-2xl bg-[hsl(var(--primary))]/10 px-3 py-2 font-semibold text-[hsl(var(--primary))]">${p.code}</div><h3 className="font-semibold">${p.projectName}</h3><p className="mt-2 text-sm text-[hsl(var(--muted))]">Client: ${p.clientName}</p><p className="mt-1 text-sm text-[hsl(var(--muted))]">Owner: ${p.projectOwner || 'Others'}</p><p className="mt-1 text-sm text-[hsl(var(--muted))]">${p.location || '-'}</p><p className="mt-4 text-xs text-[hsl(var(--muted))]">${m.progress}% progress · ${m.completed} completed / ${m.pending} pending</p></${Link}><div className="mt-4 flex gap-2"><button onClick=${()=>{setEditing(p.code); setEditForm({...p, newOwner:''});}} className="rounded-2xl border border-[hsl(var(--border))] px-4 py-2 text-sm font-semibold">Edit</button><button onClick=${()=>remove(p.code)} className="rounded-2xl border border-[hsl(var(--red))]/40 px-4 py-2 text-sm font-semibold text-[hsl(var(--red))]">Delete</button></div></div>`}</div>`; })}</div>`}</div>`;
}
